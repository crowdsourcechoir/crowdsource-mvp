---
description: Gmail reconnect is OAuth + Resume sending (SQL is optional)
alwaysApply: true
---

# Gmail — reconnect (OAuth is already configured in Vercel)

Production `GET /api/sales/gmail/status` is `configured: true`. Same-contact multi-send is guarded in code. Resume sending does **not** require a SQL migration (uses a scopes marker if `sends_enabled` is missing).

## Joel still needs to do

1. Open `https://app.crowdsourcechoir.com/admin/sales` → **Connect Gmail** → Google consent.
2. Click **Resume sending**. Reconnect alone stays paused.
3. Send from the queue with **Send** → **Yes, send now**. Pause sending anytime without disconnecting.

Skip the Supabase SQL Editor. The unique-index script timed out and is optional.

Emergency off: **Pause sending**, **Disconnect**, or Vercel `SALES_GMAIL_SENDS_ENABLED=false`.

When Gmail is connected and sending is resumed, delete or update this rule so it stops surfacing as pending.
